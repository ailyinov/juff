<!doctype html>
<html lang="en">
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <title>BeeJee Test</title>
</head>
<body>
{#{% block body %}#}
<div class="container">
    <nav class="nav nav-pills">
        <a class="nav-link {% if nav == "tasks" %} active {% endif %}" href="/">Tasks</a>
        <a class="nav-link {% if nav == 'add' %} active {% endif %}" href="/add">Add task</a>
        {% if current_user.getRole() == 'admin' %}
            <a class="nav-link {% if mnv == 'login' %} active {% endif %}" href="/log-out">Log out</a>
        {% else %}
            <a class="nav-link {% if mnv == 'login' %} active {% endif %}" href="/auth">Log in</a>
        {% endif %}
    </nav>
